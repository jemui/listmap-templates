Jeanette Mui, jemui@ucsc.edu
Asg3: Listmap Templates

Reads in input from the command line or file. The key and value are stored in
the listmap and printed out. 
'=' input prints out all the keys and values in lexicographical order. 
'name of key' prints out the pairs with that key if it exists.
'name of value' prints out all the pairs with that value if it exists.

Usage: 
./keyvalue file or input

Starter code probvided by Wesley Mackey.

Files Submitted (12)
debug.cpp
debug.h
listmap.h
listmap.tcc (edited)
main.cpp (edited)
Makefile
README
util.cpp
util.h
util.tcc
xless.h
xpair.h
